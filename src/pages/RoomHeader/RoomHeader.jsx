import React, { useContext } from 'react';
import { Context } from '../../Context';

function RoomHeader() {

    const { room, dm, who } = useContext(Context);

    return (
        <div>

        </div>
    )
}

export default RoomHeader
